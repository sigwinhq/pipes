service: pipes
frameworkVersion: '>=2.7'

provider:
    lambdaHashingVersion: 20201221
    name: aws
    region: eu-west-1
    stage: prod
    runtime: provided.al2
    environment:
        APP_ENV: prod
    httpApi:
        disableDefaultEndpoint: true
        useProviderTags: true
    tags:
        vendor: sigwin
        project: pipes

constructs:
    routes:
        type: database/dynamodb-single-table

plugins:
  - ./vendor/bref/bref
  - serverless-lift
functions:
    web:
        handler: App\Infrastructure\Symfony\Kernel
        timeout: 1
        layers:
            - ${bref:layer.php-81}
        environment:
            AWS_REGION: ${provider:region}
            AWS_DYNAMODB_TABLENAME: ${construct:routes.tableName}
            BREF_LOOP_MAX: 100
        events:
            -
                httpApi: '*'

package:
    patterns:
        # Excluded files and folders for deployment
        - '!.idea/**'
        - '!.infra/**'
        - '!assets/**'
        - '!node_modules/**'
        - '!public/build/**'
        - '!tests/**'
        - '!var/**'
        - '!.dockerignore'
        - '!.env'
        - '!.php-version'
        - '!composer.lock'
        - '!Makefile'
        - '!symfony.lock'
        # If you want to include files and folders that are part of excluded folders,
        # add them at the end
        - 'var/cache/prod/**'
        - 'public/build/entrypoints.json'
        - 'public/build/manifest.json'
